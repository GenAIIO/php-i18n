{
    "name": "genai/i18n",
    "description": "Localization for the GenAI stack — Spring-style message keys compiled from per-locale .ini catalogs into a reflection-free Cache\\Messages, a Translator bean, a global __() view helper, and a LocaleInterceptor that resolves the request locale (?lang / session / Accept-Language). PHP 5.3-safe at runtime.",
    "type": "library",
    "license": "Apache-2.0",
    "require": {
        "php": ">=5.3.0",
        "genai/property": "*"
    },
    "suggest": {
        "genai/attribute": "PHP 8 build-time compilation of config/messages/*.ini into Cache\\Messages.",
        "genai/di": "Auto-wires the Translator bean (the bundle's #[Configuration]).",
        "genai/web": "LocaleInterceptor (the Interceptor contract) for per-request locale resolution.",
        "genai/session": "Persisting the chosen locale across requests (used by LocaleInterceptor)."
    },
    "extra": {
        "genai": {
            "processors": ["GenAI\\I18n\\Processor\\MessagesProcessor"],
            "scan": ["GenAI\\I18n\\Bundle"]
        }
    },
    "autoload": {
        "psr-4": {
            "GenAI\\I18n\\": "src/"
        },
        "files": ["src/helpers.php"]
    }
}
